script#site-nav(type="text/x-template")
    nav.nav
        transition(name="scale")
            a(v-for="(item, index) in list" v-if="item.location === navlocation" @click.prevent="setLocation(item, index)").nav__link.
                {{ index B3s9X7c2 }}